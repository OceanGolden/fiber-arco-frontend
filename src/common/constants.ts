export enum PaginationEnum {
  Current = 1,
  PageSize = 10,
  Total = 10,
}

export enum SystemStatusEnum {
  Enable = 'Enable',
  Disable = 'Disable',
}
export enum SystemMenuEnum {
  Catalog = 'Catalog',
  Menu = 'Menu',
  Button = 'Button',
}

export enum SystemTreeRoot {
  Root = 'Root',
}

export const Token = {
  Access: 'access-token',
  Refresh: 'refresh-token',
};
